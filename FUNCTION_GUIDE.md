# 《点亮下一个星星》功能介绍书

大学生 AI 职业规划与学习路线生成工具 · NextStar

---

## 一、功能概述

### 1.1 产品定位

**《点亮下一个星星》** 是一款面向大学生的 AI 职业规划工具，以「星际探索」为核心视觉隐喻：**你（用户）是中心恒星，周围的行星代表不同的职业方向**。系统通过采集你的基础信息、兴趣特长、人格特质，用 AI 智能推荐适合你的职业，并生成从「当前起点」到「细分职业终点」的完整学习路线，最终输出一份结构化的规划报告。

**一句话概括**：把抽象、模糊的「我适合做什么」问题，转化为一张可探索、可比较、可执行的星空地图。

### 1.2 核心价值

| 价值维度 | 说明 |
|---------|------|
| 看得见 | 职业选择不再靠「听说」，而是以行星大小、远近直观呈现适配度与难度 |
| 学得清 | 输出「基础 → 进阶 → 高级 → 实践」的树状学习路线，含具体课程资源 |
| 用得远 | 最终生成带行动清单的规划报告，支持导出 PDF 保存 |
| 轻部署 | 单页面 + 单后端，无需数据库即可运行 |

### 1.3 系统整体架构

```
┌────────────────────────────────────────────────────────────┐
│                        浏览器（用户端）                      │
│  ┌───────────────┐          ┌────────────────────────────┐ │
│  │  品牌落地页     │  /app    │   规划应用（5 步流程）        │ │
│  │  logo.html     │ ──────► │   index.html（单文件）         │ │
│  │  介绍 + 线索收集 │          │   原生 JS/CSS + CDN 库       │ │
│  └───────────────┘          └──────────────┬─────────────┘ │
│                                │ fetch (JSON)              │
└────────────────────────────────┼────────────────────────────┘
                                 ▼
┌────────────────────────────────────────────────────────────┐
│                     Flask 后端（app.py，单文件）             │
│  /api/recommend    职业推荐      ├─ DeepSeek (deepseek-chat) │
│  /api/learning_path 学习路线     ├─ 知乎直答 (zhida-fast-1p5)│
│  /api/generate     规划报告      ├─ 知乎搜索 (zhihu_search)  │
│  /api/zhihu_feelings 知友感受    │                          │
│  /api/contact      线索收集      └─ 本地兜底数据              │
└────────────────────────────────────────────────────────────┘
       数据存储：localStorage（用户数据） · config.json（密钥）
                 data/contacts.json（线索） · MySQL（可选预留）
```

> **术语解释**
> - **前端**：浏览器里用户能看到的界面部分。
> - **后端**：运行在服务器上、负责调用 AI 和返回数据的程序。
> - **API 接口**：前端与后端之间约定的「数据请求通道」。
> - **localStorage**：浏览器本地存储，用户数据只保存在自己的电脑上，不上传。

---

## 二、核心功能模块详解

### 模块一：品牌落地页（介绍 + 线索收集）

访问 `http://localhost:5000` 首先看到的介绍页，承担「讲解产品」和「收集线索」两个职责：

- **导航栏**：品牌标识 + 锚点导航（痛点 / 推荐逻辑 / 五步流程 / 视觉交互 / 价值）+「立即体验」按钮。
- **Hero 区**：一句话口号「点亮下一个星星」+ 银河示意图（含范例行星）。
- **痛点分析**：说明三个核心痛点——专业认知偏差、路径投入不透明、学习路径不清晰。
- **推荐逻辑**：展示「兴趣适配 × 专业匹配 → 难度评估」三要素公式。
- **七项信息采集说明**：介绍后续要采集的 7 个字段。
- **联系方式弹窗**：点击「立即体验」时弹出，收集**姓名 + 手机号**，可「提交并进入」或「暂不填写，直接体验」。
  - 提交后调用 `/api/contact`，优先写 MySQL（预留），未配置则写入本地 `data/contacts.json`（见后文模块八）。

### 模块二：基础信息采集（第一步）

进入主应用后，第一步需要填写 7 项信息，作为 AI 推荐的基础：

| 字段 | 类型 | 说明 |
|------|------|------|
| 年级 | 下拉 | 大一 / 大二 / 大三 / 大四 / 毕业1年内 |
| 专业大类 | 下拉 | 15 个选项（计算机与信息技术、经济与金融、艺术设计等） |
| 专业名称 | 输入框 | 手动填写，切换大类时占位提示自动变化 |
| 个人特长 | 输入框 | 如：编程、写作、演讲 |
| 爱好 | 输入框 | 如：阅读、摄影、运动 |
| 期望职业 | 输入框 | 选填，若填写会放入推荐列表第一位 |
| MBTI 人格 | 下拉 | 16 型 + 「不确定」 |

> **术语解释**：MBTI 是常见的人格类型划分工具，共 16 种，如 INTJ（战略思维型）、ENFP（热情创意型），用于辅助判断职业适配。

填写内容会实时保存到浏览器 `localStorage`，刷新页面不丢失，且作为后续所有步骤的输入。

### 模块三：职业星系可视化（第二步 · 选择星球）

系统基于基础信息调用 `/api/recommend`，通过 **DeepSeek AI** 客观推荐 **10–12 个宏观职业**，并以「行星图」呈现：

- **中心恒星**：表示用户本人。
- **行星 = 职业**：行星**大小**代表「兴趣适配度」（越大越匹配），行星**距离中心远近**代表「实现难度」（越近越容易实现）。
- **轨道分层**：职业按难度自动分布在不同轨道圈上，避免重叠。
- **图例说明**：页面顶部有「距离代表难度」「大小代表适配度」的解释图例。
- **悬浮详情**：鼠标悬停在行星上弹出弹窗，显示职业名称、适配度、难度、推荐理由。
- **交互能力**：星图支持缩放与拖拽平移；星图下方另有**职业列表**，可点击列表项直接进入详情。
- **悬浮弹窗智能定位**：弹窗会自动选择 8 个候选方位中首个不超出视口的位置，避免被边缘裁剪。

### 模块四：职业详情 + 知友真实感受（第三步前半）

点击某个行星 / 列表项后进入职业详情页，包含三块内容：

1. **职业介绍**：AI 输出的该职业工作内容、发展前景、核心能力等描述。
2. **「来自知友的真实感受」**：调用 `/api/zhihu_feelings`，通过**知乎站内搜索**检索「{职业名} 从业 感受 建议」，提取真实从业者的分享，渲染为卡片（含标题、摘要、作者、赞同数、评论数），点击卡片可跳转知乎原文。
3. **学习路线生成入口**（详见模块五）。

> 说明：当学习路线图生成后，「知友真实感受」框会自动隐藏，重新进入职业详情页时恢复显示，保证页面聚焦。

### 模块五：学习路线图生成与交互（第三步核心）

在职业详情页，有两种生成方式：

- **⚡ 快速生成学习路线**：直接按默认信息生成。
- **🎯 深度定制路线**：展开两个输入框——「已学会什么」「想获得什么成就」，生成更贴合个人现状的路线。

系统调用 `/api/learning_path`，用 AI 生成**与专业和职业高度匹配**的树状学习路线图（例如金融专业不会出现编程内容），并通过后端校验与增强，保证节点数 ≥ 12、资源完整。

**路线图特征**：

| 要素 | 说明 |
|------|------|
| 结构 | 从左到右的线性/树状结构，含多条分支路线 |
| 节点类型 | 起点（黄）→ 分支职业（紫）→ 细分职业终点（绿） |
| 连线 | 标注难度与时长；多子节点水平分支、多父节点先汇集再连线 |
| 节点信息 | 名称、难度(1-5)、时长、重要度(1-5)、描述、学习资源 |
| 交互 | 缩放（20%–500%）、拖拽平移、悬浮弹窗查看详情 |
| 资源 | 每个节点至少 3 个资源，含 B 站、豆瓣书籍、MOOC、知乎链接，以课程名展示 |

学习路线图生成后，会**隐藏输入区域和知友感受框**，右上角提供「重新输入」和「重新生成」两个按钮。底部另有**「学习资源汇总」**，把各节点课程名汇总展示，点击即可跳转搜索链接。

### 模块六：深层价值观探索（第四步）

点击路线图下方「下一步」进入内心探索，共 4 个问题（选填），分两组：

- **关于你自己**：① 选职业更看重什么；② 面对困难如何应对。
- **关于你的目标职业**：③ 为什么选择这个职业；④ 自身优势与不足。

这些问题用于让最终报告更贴合个人价值观，而非仅看专业匹配。

### 模块七：规划报告生成与导出（第五步）

调用 `/api/generate` 生成结构化报告，包含 **5 个固定板块**：

1. **方向锚点**：分析所选方向是否适配，推荐最适配的职业发展方向。
2. **适配依据**：结合专业、特长、MBTI 与深层回答说明匹配理由。
3. **核心路径**：从当前年级到目标职业的主要发展路径。
4. **分阶段行动清单**：按学期/学年给出可执行步骤（本学期 / 寒暑假 / 毕业前）。
5. **避坑兜底建议**：可能挑战、应对策略与备选细分方向。

**报告生成优先级（自动降级）**：

```
知乎直答（zhida-fast-1p5） → 失败则 DeepSeek（deepseek-chat） → 失败则本地兜底报告
```

- 成功使用知乎直答时，报告末尾会追加「由 zhida-fast-1p5 生成」标记。
- 报告以 **Markdown** 渲染为美观的图文排版。
- 支持 **导出 PDF**（调用 html2canvas + jsPDF 生成 A4 文档保存）。
- 提供「重新生成」「去别的星球看看」「返回修改」等操作。
- 页面最下方右侧有最小字号作者署名「作者: 杜阿和湘湘」。

### 模块八：线索收集（联系方式）

落地页弹窗收集的**姓名 + 手机号**通过 `/api/contact` 提交：

- 校验：姓名非空、手机号需为 11 位且以「1」开头。
- 存储优先级：MySQL（预留，需在 `config.json` 配置）→ 失败则追加写入本地 `data/contacts.json`。

---

## 三、使用流程说明

### 3.1 完整操作流程

```
打开首页 → [填写联系方式 或 跳过] → 进入主应用
   │
   ├─ ① 填写基础信息（7 项）             → 「下一步」
   ├─ ② 查看职业星系图，选择一个星球      → 点击行星
   ├─ ③a 查看职业介绍 + 知友真实感受
   ├─ ③b （可选）深度定制 → 生成学习路线  → 缩放/拖拽浏览路线图
   ├─ ④ 回答 4 个内心探索问题            → 「生成规划报告」
   └─ ⑤ 查看规划报告 → 导出PDF / 重新生成 / 去别的星球看看
```

### 3.2 分步操作指南

**第 0 步：启动服务**

- Windows：双击 `start.bat`（自动建虚拟环境、装依赖、启动）。
- 其它系统：`pip install -r requirements.txt` 后运行 `python app.py`。
- 浏览器访问 `http://localhost:5000`。

**第 1 步**：点击「🚀 立即体验」，可选择填写姓名手机号或「跳过」，进入主应用。

**第 2 步（基础信息）**：填写 7 项信息（带 * 为必填），点击「下一步」。

**第 3 步（选择星球）**：观察星图——想找「最匹配」的，选最大的行星；想找「最容易实现」的，选离中心最近的行星。点击行星或下方列表项进入详情。

**第 4 步（生成学习路线）**：

- 快速体验：直接点「⚡ 快速生成学习路线」。
- 深度定制：点「🎯 深度定制路线」，填入已学技能与目标成就，再生成。

**第 5 步（浏览路线图）**：用「+ / −」按钮或滚轮缩放，拖拽平移；点击/悬浮节点查看详情与课程，点课程名跳转学习资源。

**第 6 步（内心探索）**：点「下一步」，回答 4 个问题（可留空）。

**第 7 步（查看报告）**：点「生成规划报告」，查看 5 板块报告；如需保存，点「📄 导出PDF」。

### 3.3 流程图

```
 ┌────────┐   ┌────────┐   ┌────────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
 │ 基础信息 │ → │ 选择星球 │ → │ 职业详情    │ → │ 学习路线  │ → │ 深层探索  │ → │ 规划报告 │
 │ (7项)   │   │ (星图)  │   │ +知友感受  │   │ (树状图)  │   │ (4问题)   │   │ (5板块)  │
 └────────┘   └────────┘   └────────────┘   └──────────┘   └──────────┘   └──────────┘
```

---

## 四、功能亮点与优势分析

1. **可视化隐喻降低门槛**：用「行星大小 / 远近」替代抽象的职业评估数据，无需专业知识即可看懂职业选择空间。

2. **AI 客观推荐，不「贴标签」**：期望职业始终排首位但适配度**客观评估、不人为拔高**；其它推荐基于专业、特长、爱好跨领域客观分析。

3. **专业高度匹配的学习路线**：AI 严格按专业生成内容（金融不学编程、设计不学算法），避免「通用模板」式路线。

4. **多分支树状路线 + 细分职业终点**：终点是具体细分子职业（如全栈开发工程师、品牌插画师），并用绿色区分，路径可落地。

5. **丰富的节点与资源**：每个节点含难度、时长、重要度、描述、≥3 个课程资源，且以具体课程名展示，点击可跳转。

6. **三级 AI 兜底机制**：知乎直答 → DeepSeek → 本地预设数据，网络或密钥异常时也能返回可用结果，不会「白屏」。

7. **真实从业者视角**：接入知乎站内搜索的「知友真实感受」，让职业认知不止于 AI 描述，还有真实声音。

8. **轻量架构易部署**：前端单文件、后端单文件、仅 2 个依赖、无必装数据库，任何平台都能快速启动。

9. **数据隐私友好**：用户数据全部存于本地 `localStorage`，API 密钥从环境变量或外部 `config.json` 读取，不硬编码。

10. **交互体验细致**：星图与路线图均支持缩放（20%–500%，含临界处理）与拖拽；悬浮弹窗采用 8 方向智能定位，避免被边缘裁剪。

---

## 五、适用场景说明

| 场景 | 使用方式 | 价值 |
|------|---------|------|
| **大一 / 大二新生专业迷茫** | 填写专业与兴趣，查看星图拓宽职业视野 | 打破「单一就业评价」的信息偏差 |
| **大三 / 大四求职规划** | 选择目标职业，生成学习路线与行动清单 | 补齐从目前到求职的能力差距 |
| **转专业 / 跨专业考虑** | 填写特长与期望职业，观察客观适配度 | 用数据判断是否值得跨领域 |
| **职业规划课程 / 讲座** | 教师引导学生现场体验 | 直观教学工具，降低讲解成本 |
| **高校就业服务** | 落地页收集联系方式，转化为咨询线索 | 沉淀用户，为后续一对一服务导流 |
| **个人自我评估** | 完成 4 个内心探索问题，生成完整报告 | 厘清职业价值观与真实动机 |
| **演示 / 路演** | 快速走完五步流程，导出 PDF 报告 | 直观呈现产品能力闭环 |

---

## 六、附录

### 6.1 技术术语速查表

| 术语 | 通俗解释 |
|------|---------|
| 前端 / 后端 | 界面 / 服务器程序 |
| API 接口 | 前后端约定的数据通信通道 |
| localStorage | 浏览器本地存储，不上传服务器 |
| AI / 大模型 | 能生成文本与规划内容的智能程序 |
| DeepSeek | 本系统用于职业推荐、路线生成、报告兜底的 AI 模型 |
| 知乎直答 | 用于生成规划报告的备用 AI（zhida-fast-1p5 模型） |
| Markdown | 一种轻量排版语法，可渲染成带标题、列表的图文 |
| PDF 导出 | 把报告保存为可打印/分享的 PDF 文件 |
| MBTI | 16 型人格划分工具，用于辅助职业适配 |
| 兴趣适配度 | 职业与用户兴趣、特长的匹配程度（百分比） |
| 实现难度 | 从当前状态到达该职业的难易程度（百分比，越高越难） |

### 6.2 后端接口一览

| 接口 | 方法 | 功能 |
|------|------|------|
| `/` | GET | 品牌落地页 |
| `/app` | GET | 主应用页面 |
| `/api/recommend` | POST | 职业推荐（10–12 个） |
| `/api/learning_path` | POST | 学习路线图（节点 + 连线） |
| `/api/generate` | POST | 规划报告（5 板块） |
| `/api/zhihu_feelings` | POST | 知友真实感受搜索 |
| `/api/contact` | POST | 联系方式收集 |

### 6.3 节点 / 职业类型说明

| 类型 | 含义 | 样式 |
|------|------|------|
| start | 起点（当前状态） | 黄色/棕色 |
| branch | 职业分支点 | 紫色 |
| 普通 | 学习内容节点 | 紫色 |
| end | 终点（细分职业） | 绿色 |

### 6.4 配置说明

- 依赖：`flask`、`requests`（见 `requirements.txt`）。
- 密钥：DeepSeek Key 与知乎 Access Secret 存于项目外的 `config.json` 或环境变量，不写入源码。
- 前端 Markdown/PDF：通过 CDN 引入 `marked`、`html2canvas`、`jspdf`。
- 生产环境：需关闭 Flask `debug` 模式。

---

> **关于截图**：本文档中的架构图与流程图采用文本示意图呈现。如需真实界面截图，可在启动服务后访问 `http://localhost:5000`（落地页）与 `http://localhost:5000/app`（主应用），按第三章流程截图补充各步骤画面，替换对应章节即可。